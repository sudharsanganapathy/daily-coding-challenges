// Create a function that accepts the input array
// Since a uniform parity array can always be constructed, return true
// Test the function with arrays containing odd numbers
// Test the function with arrays containing even numbers
// Test the function with a mixture of odd and even numbers
// Return the result for each test case


function uniformParityArray(nums1){
    return true;
}

console.log(uniformParityArray([1, 3]));
console.log(uniformParityArray([2, 4, 6, 8]));
console.log(uniformParityArray([1, 3, 5, 7]));
console.log(uniformParityArray([2, 3, 6, 9]));